specVersion: "v1beta"
serviceId: "straya-visuals"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "straya-visuals-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
